# 개발 모드(npm run dev)에서는 Vite 프록시를 통해 /api/v1 로 Backend에 연결합니다.
# Vite가 프록시할 Backend 주소 (기본값: http://127.0.0.1:8000)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
#
# 프로덕션 빌드에서만 직접 Backend URL을 지정하세요.
# VITE_API_BASE_URL=http://backend-host:8000/api/v1
